const { people, ages } = require("./people");

console.log(people, ages);

// This will help you to get the information of the current operating system.
const os = require("os");
console.log(os.platform(), os.homedir());
